<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.For_Chour_Kotlin" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color.-->
        <item name="colorPrimary">@color/whitenIvisible</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:navigationBarColor">@color/whitenIvisible</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/whitenIvisible</item>
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
    <style name="Theme.For_Chour_Kotlin.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.For_Chour_Kotlin.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.For_Chour_Kotlin.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="MyAlertDialogTheme" parent="Theme.MaterialComponents.Dialog.Alert">
        <!-- Дополнительные настройки, если необходимо -->
        <item name="colorAccent">@color/teal_700</item> <!-- Цвет акцента -->
    </style>

    <style name="CustomButton" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/button_in_out</item>
        <item name="backgroundTint">@null</item>
        <item name="cornerRadius">8dp</item> <!-- если хотите -->
    </style>
</resources>